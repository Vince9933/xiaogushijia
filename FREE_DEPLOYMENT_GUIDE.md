# 免费部署方式指南

针对您的React+Vite项目，以下是**完全免费**的部署方案，无需支付任何服务器费用：

## 📌 推荐方案：Vercel（最适合React/Vite项目）

### 步骤1：准备GitHub仓库
1. 注册GitHub账号：https://github.com/
2. 创建一个新仓库（仓库名任意）
3. 将项目文件上传到GitHub仓库

### 步骤2：部署到Vercel
1. 注册Vercel账号：https://vercel.com/（可直接用GitHub登录）
2. 点击**New Project** → **Import Git Repository**
3. 选择您的GitHub仓库 → 点击**Import**
4. **配置项目**：
   - Framework：选择 `Vite`
   - Build Command：保持默认（`npm run build`）
   - Output Directory：`dist/static`
   - Environment Variables：无需设置
5. 点击**Deploy**，等待部署完成

### 步骤3：访问网站
- 部署完成后，Vercel会提供一个免费域名（如：`your-project.vercel.app`）
- 直接访问该域名即可

## 📌 备选方案1：Netlify

### 步骤1：准备GitHub仓库（同上）

### 步骤2：部署到Netlify
1. 注册Netlify账号：https://www.netlify.com/
2. 点击**Add new site** → **Import an existing project**
3. 选择GitHub → 授权并选择您的仓库
4. **配置构建**：
   - Build command：`npm run build`
   - Publish directory：`dist/static`
5. 点击**Deploy site**

## 📌 备选方案2：GitHub Pages

### 步骤1：修改vite.config.ts
```typescript
// vite.config.ts
export default defineConfig({
  // ...其他配置
  base: '/仓库名称/', // 例如：'/my-project/'
});
```

### 步骤2：创建gh-pages脚本
在package.json中添加：
```json
"scripts": {
  // ...其他脚本
  "deploy:github": "npm run build && npx gh-pages -d dist/static"
}
```

### 步骤3：部署
```bash
npm run deploy:github
```

### 步骤4：访问
- 域名：`https://用户名.github.io/仓库名称/`

## 📌 备选方案3：Cloudflare Pages

### 步骤1：准备GitHub仓库（同上）

### 步骤2：部署到Cloudflare Pages
1. 注册Cloudflare账号：https://pages.cloudflare.com/
2. 点击**Create a project** → 选择GitHub
3. 选择您的仓库 → 点击**Begin setup**
4. **配置构建**：
   - Framework preset：`Vite`
   - Build command：`npm run build`
   - Build output directory：`dist/static`
5. 点击**Save and Deploy**

## 💡 项目适配说明

### 1. 移除Express服务器依赖
您的项目包含`server.cjs`文件，但对于免费静态部署平台，**不需要这个文件**。平台会自动处理路由和静态文件。

### 2. 构建命令调整
- Vercel/Netlify/Cloudflare：使用 `npm run build`
- GitHub Pages：需要修改`base`配置

### 3. 路由注意事项
- 如果使用React Router，需要确保路由配置支持静态部署
- 在`vite.config.ts`中设置正确的`base`路径（仅GitHub Pages需要）

## 📋 各方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Vercel** | React/Vite最佳支持，自动部署，性能好 | 无明显缺点 |
| **Netlify** | 配置简单，支持函数，CDN速度快 | 构建速度略慢 |
| **GitHub Pages** | 完全免费，与GitHub集成 | 需手动配置base路径 |
| **Cloudflare Pages** | 全球CDN，速度极快 | 控制台略复杂 |

## 🚀 快速部署建议

**最快的方式**：使用Vercel + GitHub

1. 创建GitHub仓库 → 上传代码
2. Vercel导入仓库 → 一键部署
3. 获得免费域名，立即访问

整个过程**不超过5分钟**，完全免费且无需任何服务器配置！

## ❓ 常见问题

### Q: 需要安装额外的依赖吗？
A: 不需要，项目已有的依赖完全足够。

### Q: 免费域名可以自定义吗？
A: Vercel/Netlify支持绑定自己的域名（需购买域名），但免费提供的域名也可正常使用。

### Q: 部署后可以更新代码吗？
A: 可以，每次push代码到GitHub，平台会自动重新构建部署。

### Q: 有流量限制吗？
A: 免费方案有一定的流量限制，但对于个人项目完全足够（Vercel免费版：100GB/月）。

---

**提示**：如果您对这些平台不熟悉，推荐先尝试**Vercel**，它对React/Vite项目的支持最好，配置最简单！